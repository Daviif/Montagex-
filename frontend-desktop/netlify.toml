[build]
  base = "frontend-desktop"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing — todas as rotas redirecionam para index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
